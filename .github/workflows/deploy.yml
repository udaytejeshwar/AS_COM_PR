name: Deploy to GitHub Pages

on:
  push:
    branches:
      - main  # or your default branch

permissions:
  contents: write  # required to push to gh-pages branch

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Set up Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '20'

      - name: Install dependencies
        run: npm install

      - name: Build site with Vite
        run: npm run build

      - name: Create CNAME file
        run: echo 'arkspindles.com' > dist/CNAME

      - name: Configure Git identity
        run: |
          git config --global user.name "github-actions bot"
          git config --global user.email "github-actions@users.noreply.github.com"

      # - name: Deploy to GitHub Pages
      #   env:
      #     GH_TOKEN: ${{ secrets.AS_PRIVATE_DEPLOY_TOKEN }}
      #   run: |
      #     npx gh-pages -d dist \
      #       -u "github-actions bot <github-actions@users.noreply.github.com>" \
      #       -r "https://x-access-token:${GH_TOKEN}@github.com/udaytejeshwar/AS_COM_PR.git"
